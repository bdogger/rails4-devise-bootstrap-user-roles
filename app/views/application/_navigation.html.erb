<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Company, LLC</a></div>
        <% if user_signed_in? %>
            <div class="navbar-collapse collapse">
                <% if current_user.has_role?(:employee) %>
                    <ul class="nav navbar-nav">
                        <li class=""><%= link_to 'Home', root_url %></li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Employee Only Tab<b class="caret"></b></a>
                            <ul class="dropdown-menu customers-menu">
                                <li><a href="#">foo</a></li>
                            </ul>
                        </li>
                        <% if current_user.has_role?(:admin) %>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin<b class="caret"></b></a>
                                <ul class="dropdown-menu admin-menu">
                                    <li><%= link_to 'Users', administer_users_path %></li>
                                </ul>
                            </li>
                        <% end %>
                    </ul>
                <% end %>
                <div class="navbar-form navbar-right">
                    <%= link_to 'Update Password', edit_user_registration_path, class: 'btn btn btn-info' %>
                    <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
                </div>
            </div>
        <% end %>
        <!--/.navbar-collapse -->
    </div>
</div>
